# etmem-operator 快速上手手册（其他机器部署指南）

> 本文档面向**初次接触 etmem-operator** 的用户，逐步指导您在一台**新机器**上完成部署和验证。
> 每一步都包含：**做什么 → 怎么做 → 预期输出 → 异常处理**。

---

## 目录

1. [环境准备（硬件/软件要求）](#1-环境准备硬件软件要求)
2. [环境检查（检查所有前提条件）](#2-环境检查检查所有前提条件)
3. [获取代码与构建](#3-获取代码与构建)
4. [构建镜像](#4-构建镜像)
5. [部署 etmem-operator（Helm）](#5-部署-etmem-operatorhelm)
6. [验证部署成功](#6-验证部署成功)
7. [创建测试工作负载](#7-创建测试工作负载)
8. [启用 etmem（添加 label）](#8-启用-etmem添加-label)
9. [验证 etmem 生效](#9-验证-etmem-生效)
10. [切换 profile](#10-切换-profile)
11. [清理与卸载](#11-清理与卸载)
12. [常见问题排查](#12-常见问题排查)

---

## ⚠️ 重要说明：「本地验证」与「真实 swap 验证」的边界

etmem-operator 的功能分为两层：

| 层级 | 说明 | 在哪验证 |
|------|------|----------|
| **控制面** | Operator 监听 Pod label → 自动创建 EtmemPolicy；Agent 匹配 Pod → 解析 PID → 生成配置 | **任意 K8s 集群**（包括 Kind、Minikube、单节点集群等） |
| **数据面** | etmemd 扫描冷页 → 写入 swap → 可用内存增长 | **仅限真实目标环境**（openEuler/FusionOS 内核 + etmem 内核模块 + swap 设备） |

- 如果您的机器**没有** openEuler 内核和 etmem 内核模块，您仍然可以验证**控制面**的全部流程。
- 如果您的机器**有** openEuler 内核和 etmem 内核模块，您可以验证**完整的数据面**，即真正的内存扫描和 swap 操作。

本手册会在每个步骤中明确标注该步骤属于哪个层级。

---

## 1. 环境准备（硬件/软件要求）

### 1.1 硬件要求

| 资源 | 最低要求 | 建议 |
|------|----------|------|
| CPU  | 2 核     | 4 核+ |
| 内存 | 4 GB     | 8 GB+ |
| 磁盘 | 20 GB 可用空间 | 40 GB+ |
| 网络 | 能访问互联网（拉取基础镜像） | — |

### 1.2 软件要求

| 软件 | 版本要求 | 用途 |
|------|----------|------|
| **操作系统** | openEuler 22.03+ 或 FusionOS 23（数据面验证必需）；其他 Linux 发行版可验证控制面 | 提供 etmem 内核模块 |
| **Kubernetes** | 1.26+ | 集群运行环境 |
| **Helm** | 3.x | 部署 Operator 和 Agent |
| **Go** | 1.21+ | 编译源码 |
| **containerd** | 1.6+ | 容器运行时 |
| **nerdctl** 或 **docker** | 最新版 | 构建容器镜像 |
| **git** | 任意版本 | 获取源码 |

### 1.3 数据面额外要求（仅 openEuler/FusionOS 内核需要）

| 条件 | 说明 |
|------|------|
| 内核模块 `etmem_scan`、`etmem_swap` | 必须加载 |
| `etmemd` 二进制 | 必须安装并在每个 Worker 节点运行 |
| swap 设备 | 必须启用（`swapon -s` 应有输出） |
| cgroup v1 | 当前版本仅支持 cgroup v1（不支持 cgroup v2） |

---

## 2. 环境检查（检查所有前提条件）

> 在开始部署之前，请逐项检查以下条件。任何一项不满足都可能导致部署失败或功能受限。

### 2.1 检查 Kubernetes 集群

**做什么：** 确认集群可用且版本满足要求。

```bash
kubectl version --short 2>/dev/null || kubectl version
```

**预期输出（示例）：**
```
Client Version: v1.28.2
Server Version: v1.28.2
```

**异常处理：**
- 如果提示 `command not found`：请先安装 kubectl。
- 如果 Server Version 为空或报错：请确认集群已启动（`systemctl status kubelet`）。
- 如果版本低于 1.26：建议升级集群。

### 2.2 检查 Helm

**做什么：** 确认 Helm 3 已安装。

```bash
helm version --short
```

**预期输出（示例）：**
```
v3.13.1+g3547a4b
```

**异常处理：**
- 如果提示 `command not found`：请安装 Helm 3（参考 https://helm.sh/docs/intro/install/）。

### 2.3 检查 Go 编译器

**做什么：** 确认 Go 版本满足要求（1.21+）。

```bash
go version
```

**预期输出（示例）：**
```
go version go1.21.5 linux/amd64
```

**异常处理：**
- 如果版本低于 1.21：请升级 Go（https://go.dev/dl/）。

### 2.4 检查容器运行时

**做什么：** 确认 containerd 正在运行，且 nerdctl 或 docker 可用。

```bash
# 检查 containerd
systemctl status containerd | head -5

# 检查 nerdctl（推荐）
sudo nerdctl version

# 或者检查 docker
docker version
```

**预期输出：** containerd 状态为 `active (running)`，nerdctl/docker 能正常输出版本号。

**异常处理：**
- containerd 未运行：`sudo systemctl start containerd`
- nerdctl 和 docker 都不可用：请安装其中之一。

### 2.5 检查 cgroup 版本（控制面 + 数据面）

**做什么：** 确认系统使用 cgroup v1。当前版本的 Agent **不支持 cgroup v2**。

```bash
# 方法一：检查 /sys/fs/cgroup 挂载类型
stat -fc %T /sys/fs/cgroup/
```

**预期输出：**
```
tmpfs
```

> 如果输出是 `tmpfs`，说明是 cgroup v1。如果输出是 `cgroup2fs`，说明是 cgroup v2，Agent 将**无法解析容器 PID**。

```bash
# 方法二：检查 memory cgroup 子系统是否存在
ls /sys/fs/cgroup/memory/
```

**预期输出：** 应有文件列表（`cgroup.procs`、`memory.usage_in_bytes` 等）。

**异常处理：**
- 如果是 cgroup v2：需要在内核启动参数中添加 `systemd.unified_cgroup_hierarchy=0` 切换到 v1，然后重启。
- 这是**已知限制**，v2 支持将在未来版本添加。

### 2.6 【数据面】检查 etmem 内核模块

> ⚠️ 此步骤仅在 openEuler/FusionOS 内核上可用。非 openEuler 内核可跳过，但将无法验证数据面。

```bash
# 尝试加载内核模块
sudo modprobe etmem_scan etmem_swap

# 确认加载成功
lsmod | grep etmem
```

**预期输出：**
```
etmem_swap            xxxxx  0
etmem_scan            xxxxx  0
```

**异常处理：**
- `modprobe: FATAL: Module etmem_scan not found`：您的内核不包含 etmem 模块，无法验证数据面。
- 确认您运行的是 openEuler 内核：`uname -r` 应包含 `oe` 字样。

### 2.7 【数据面】检查 etmemd 守护进程

```bash
# 检查 etmemd 是否已安装
which etmemd

# 检查 etmemd 是否正在运行
ps aux | grep etmemd | grep -v grep
```

**预期输出：**
```
/usr/bin/etmemd
root  xxxxx  ... etmemd -s etmemd_socket -l 0
```

**如果 etmemd 未运行，手动启动：**

```bash
# 后台启动 etmemd
sudo etmemd -s etmemd_socket -l 0 &

# 确认启动成功
ps aux | grep etmemd | grep -v grep
```

**异常处理：**
- `etmemd: command not found`：需要在 openEuler 上安装 etmemd 包（`yum install etmem`）。
- Agent 启动时会连接 `/var/run/etmemd_socket`，如果 etmemd 没启动，Agent 功能将受限。

### 2.8 【数据面】检查 swap 状态

```bash
swapon -s
cat /proc/swaps
free -h | grep -i swap
```

**预期输出（示例）：**
```
Filename                Type        Size      Used    Priority
/dev/sda2               partition   4194300   0       -2

              total        used        free
Swap:         4.0Gi       0B          4.0Gi
```

**如果 swap 未启用：**

```bash
# 创建 swap 文件（示例：4GB）
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

# 确认生效
swapon -s
```

**异常处理：**
- 如果 `swapon -s` 无输出：swap 未配置，etmemd 无法将冷页交换出去。

---

## 3. 获取代码与构建

### 3.1 获取源码

**做什么：** 克隆 etmem-operator 仓库。

```bash
# 如果已有代码，跳过此步
git clone <your-repo-url> etmem-operator
cd etmem-operator
```

### 3.2 编译二进制文件

**做什么：** 编译 Operator 和 Agent 的 Linux amd64 二进制文件。

```bash
# 确保在 etmem-operator 项目根目录
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o bin/operator ./cmd/operator
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o bin/agent ./cmd/agent
```

**预期输出：** 无报错，`bin/` 目录下出现 `operator` 和 `agent` 两个二进制文件。

```bash
ls -la bin/
```

**预期输出：**
```
-rwxr-xr-x 1 user user xxxxxxx  ... operator
-rwxr-xr-x 1 user user xxxxxxx  ... agent
```

**异常处理：**
- 编译失败提示缺少依赖：运行 `go mod tidy && go mod download` 后重试。
- `GOOS=linux` 是必需的，即使您在 Linux 上编译（确保交叉编译环境正确）。

---

## 4. 构建镜像

### 4.1 构建容器镜像

**做什么：** 使用 nerdctl（或 docker）构建 Operator 和 Agent 的容器镜像。

> ⚠️ **关键：** 必须使用 `--namespace k8s.io` 参数，这样 containerd（K8s 的容器运行时）才能找到镜像。

```bash
# 构建 Operator 镜像
sudo nerdctl --namespace k8s.io build -t etmem-operator:v0.2.0 -f Dockerfile.operator .

# 构建 Agent 镜像
sudo nerdctl --namespace k8s.io build -t etmem-agent:v0.2.0 -f Dockerfile.agent .
```

**预期输出：** 构建成功，最后一行类似：
```
naming to etmem-operator:v0.2.0 done
naming to etmem-agent:v0.2.0 done
```

### 4.2 确认镜像存在

```bash
sudo nerdctl --namespace k8s.io images | grep etmem
```

**预期输出：**
```
etmem-operator   v0.2.0   xxxxxx   ...
etmem-agent      v0.2.0   xxxxxx   ...
```

**异常处理：**
- 构建失败提示 Dockerfile 不存在：确认您在 `etmem-operator` 项目根目录。
- 使用 docker 的话：`docker build -t etmem-operator:v0.2.0 -f Dockerfile.operator .`，但需确保 K8s 节点能拉取到镜像。

### 4.3 多节点集群：分发镜像

> 如果您的集群有多个 Worker 节点，需要将镜像复制到每个节点。

```bash
# 导出镜像
sudo nerdctl --namespace k8s.io save etmem-operator:v0.2.0 -o etmem-operator.tar
sudo nerdctl --namespace k8s.io save etmem-agent:v0.2.0 -o etmem-agent.tar

# 复制到其他节点（替换 <node-ip>）
scp etmem-operator.tar etmem-agent.tar root@<node-ip>:/root/

# 在其他节点上导入
ssh root@<node-ip> "nerdctl --namespace k8s.io load -i /root/etmem-operator.tar"
ssh root@<node-ip> "nerdctl --namespace k8s.io load -i /root/etmem-agent.tar"
```

---

## 5. 部署 etmem-operator（Helm）

### 5.1 安装 CRDs 和组件

**做什么：** 使用 Helm 一键部署 Operator、Agent、CRDs、RBAC 等全部组件。

```bash
helm install etmem-operator deploy/helm \
  --namespace etmem-system \
  --create-namespace
```

**预期输出：**
```
NAME: etmem-operator
LAST DEPLOYED: ...
NAMESPACE: etmem-system
STATUS: deployed
REVISION: 1
```

**异常处理：**
- `Error: INSTALLATION FAILED: cannot re-use a name that is still in use`：之前已安装过，先卸载：`helm uninstall etmem-operator -n etmem-system`。
- 如果需要自定义配置（例如修改 Agent 的 reconcileInterval），可以通过 `--set` 参数：
  ```bash
  helm install etmem-operator deploy/helm \
    --namespace etmem-system \
    --create-namespace \
    --set agent.reconcileInterval=60s
  ```

### 5.2 确认 Helm Release 状态

```bash
helm list -n etmem-system
```

**预期输出：**
```
NAME              NAMESPACE      REVISION  STATUS    ...
etmem-operator    etmem-system   1         deployed  ...
```

---

## 6. 验证部署成功

> 🏷️ **层级：控制面** — 以下所有验证在任意 K8s 集群上都可执行。

### 6.1 检查 CRD 是否安装

```bash
kubectl get crd | grep etmem
```

**预期输出：**
```
etmempolicies.etmem.openeuler.io      ...
etmemnodestates.etmem.openeuler.io    ...
```

**异常处理：**
- 无输出：CRD 未安装，检查 Helm chart 的 `crds/` 目录是否包含 YAML 文件。

### 6.2 检查 Pod 状态

```bash
kubectl -n etmem-system get pods -o wide
```

**预期输出：**
```
NAME                               READY   STATUS    RESTARTS   AGE    NODE
etmem-operator-xxxxxxxxxx-xxxxx    1/1     Running   0          1m     ...
etmem-agent-xxxxx                  1/1     Running   0          1m     ...
```

**异常处理：**
- Pod 状态为 `ImagePullBackOff`：镜像未在节点上找到。确认第 4 步中镜像已构建且使用了 `--namespace k8s.io`。
- Pod 状态为 `CrashLoopBackOff`：查看日志 `kubectl -n etmem-system logs <pod-name>`。
- Pod 状态为 `Pending`：`kubectl describe pod <pod-name> -n etmem-system` 查看调度原因。

### 6.3 检查 Operator 日志

```bash
kubectl -n etmem-system logs deploy/etmem-operator --tail=20
```

**预期输出（关键行）：**
```
Starting Controller
Starting workers
```

### 6.4 检查 Agent 日志

```bash
kubectl -n etmem-system logs ds/etmem-agent --tail=20
```

**预期输出（关键行）：**
```
Starting etmem agent
Starting reconcile loop
```

---

## 7. 创建测试工作负载

> 🏷️ **层级：控制面** — 创建一个用于测试的 Pod。

### 7.1 创建测试 Namespace

```bash
kubectl create namespace etmem-test
```

### 7.2 部署测试 Pod

**做什么：** 创建一个简单的 Pod 作为 etmem 的目标工作负载。

```bash
cat <<EOF | kubectl apply -f -
apiVersion: v1
kind: Pod
metadata:
  name: memory-hog
  namespace: etmem-test
spec:
  containers:
  - name: stress
    image: busybox
    command: ["sh", "-c", "while true; do sleep 3600; done"]
    resources:
      requests:
        memory: "128Mi"
      limits:
        memory: "256Mi"
EOF
```

**预期输出：**
```
pod/memory-hog created
```

### 7.3 确认 Pod 运行

```bash
kubectl -n etmem-test get pods
```

**预期输出：**
```
NAME         READY   STATUS    RESTARTS   AGE
memory-hog   1/1     Running   0          ...
```

**异常处理：**
- 如果 Pod 拉取 busybox 镜像失败（离线环境），可以替换为节点上已有的任意镜像。

---

## 8. 启用 etmem（添加 label）

> 🏷️ **层级：控制面** — 这是用户唯一需要做的操作。

### 8.1 给 Pod 添加 etmem 标签

**做什么：** 添加 `etmem.openeuler.io/enable=true` 标签，Operator 会自动创建 EtmemPolicy。

```bash
kubectl -n etmem-test label pod memory-hog etmem.openeuler.io/enable=true
```

**预期输出：**
```
pod/memory-hog labeled
```

### 8.2 （可选）指定 profile

**做什么：** 通过 annotation 指定内存回收策略。不指定则默认使用 `aggressive`。

可选值：`conservative`、`moderate`、`aggressive`、`extreme`

```bash
kubectl -n etmem-test annotate pod memory-hog etmem.openeuler.io/profile=moderate
```

**预期输出：**
```
pod/memory-hog annotated
```

### 8.3 Profile 参数说明

| Profile | 回收力度 | 适用场景 | 系统内存阈值 |
|---------|----------|----------|-------------|
| `conservative` | 低 | 对延迟敏感的关键业务 | 系统内存使用 >70% 才触发 |
| `moderate` | 中 | 一般业务 | 系统内存使用 >50% 才触发 |
| `aggressive` | 高（默认） | 内存密集型应用 | 系统内存使用 >30% 才触发 |
| `extreme` | 极高 | 测试/极端场景 | 系统内存使用 >20% 才触发 |

---

## 9. 验证 etmem 生效

### 9.1 【控制面】检查 EtmemPolicy 自动创建

**做什么：** Operator 应自动创建对应的 EtmemPolicy 资源。

```bash
kubectl get etmempolicy -A
```

**预期输出（示例）：**
```
NAMESPACE     NAME                    AGE
etmem-test    memory-hog-policy       1m
```

> 如果无输出，等待几秒后重试（Operator 有处理延迟），或检查 Operator 日志。

### 9.2 【控制面】查看 EtmemPolicy 详情

```bash
kubectl -n etmem-test get etmempolicy -o yaml
```

关注以下字段：
- `spec.podName`：应为 `memory-hog`
- `spec.profile`：应为您指定的 profile（默认 `aggressive`）
- `spec.nodeName`：应为 Pod 所在节点名称

### 9.3 【控制面】检查 EtmemNodeState

```bash
kubectl get etmemnodestate -A
```

**说明：** Agent 会为所在节点创建/更新 EtmemNodeState 资源。

> **已知限制：** `socketReachable` 和 `etmemdReady` 字段当前**不会动态更新**（始终为 false），这是已知问题，不影响实际功能。

### 9.4 【控制面】检查 Agent 匹配日志

```bash
kubectl -n etmem-system logs ds/etmem-agent --tail=50 | grep -i "matched\|reconcil\|pid\|policy"
```

**预期输出（示例）：**
```
Matched pod memory-hog in namespace etmem-test
Found container PIDs: [12345]
```

> 如果 Agent 日志中显示 "Matched pod" 和 PID 信息，说明控制面工作正常。

### 9.5 【数据面】验证真实 swap 效果

> ⚠️ 以下步骤**仅在 openEuler/FusionOS 内核 + etmemd 运行 + swap 启用**的环境中有效。

#### 9.5.1 确认 etmemd 正在运行

```bash
# 在 Worker 节点上执行
ps aux | grep etmemd | grep -v grep
```

#### 9.5.2 检查进程的 VmSwap

```bash
# 找到容器进程的 PID（从 Agent 日志中获取，或手动查找）
# 假设 PID 为 12345
cat /proc/12345/status | grep -E 'VmSwap|VmRSS'
```

**预期输出（swap 生效后）：**
```
VmRSS:     50000 kB
VmSwap:    30000 kB
```

> `VmSwap` 大于 0 说明 etmemd 已成功将部分内存交换到 swap。

#### 9.5.3 检查系统 swap 使用情况

```bash
free -h
cat /proc/meminfo | grep -E 'MemAvailable|SwapCached|SwapTotal|SwapFree'
```

**观察重点：**
- `SwapFree` 应比 `SwapTotal` 小（说明有数据被 swap 出去了）
- `MemAvailable` 应有所增长（被 swap 出去的内存释放为可用内存）
- `SwapCached` 可能增长（表示 swap 缓存中的数据）

#### 9.5.4 如果 VmSwap 始终为 0

可能的原因：
1. etmemd 未运行或 socket 不通
2. 系统内存使用率未达到 profile 的 `sysMem` 阈值
3. swap 未启用或 swap 空间不足
4. 内核 swappiness 参数过低
5. etmem 内核模块未加载

**诊断命令：**
```bash
# 检查 swappiness
cat /proc/sys/vm/swappiness
# 建议设置为 60 或更高
sudo sysctl vm.swappiness=60

# 检查 swap_extension（openEuler 特有）
cat /proc/sys/vm/swap_extension 2>/dev/null

# 检查 etmemd socket
ls -la /var/run/etmemd_socket

# 检查 etmemd 日志
journalctl -u etmemd --tail=50
```

---

## 10. 切换 profile

### 10.1 修改 Profile

**做什么：** 更新 Pod 的 annotation 来切换回收策略。

```bash
# 切换到 conservative 策略
kubectl -n etmem-test annotate pod memory-hog \
  etmem.openeuler.io/profile=conservative --overwrite
```

**预期输出：**
```
pod/memory-hog annotated
```

### 10.2 验证 Policy 更新

```bash
# 等待几秒后检查
kubectl -n etmem-test get etmempolicy -o yaml | grep profile
```

**预期输出：** `profile` 字段应变为 `conservative`。

### 10.3 各 Profile 详细参数对照

| 参数 | conservative | moderate | aggressive | extreme |
|------|-------------|----------|------------|---------|
| loop（扫描轮次） | 3 | 1 | 1 | 1 |
| interval（扫描间隔/秒） | 5 | 1 | 1 | 1 |
| sleep（轮次间隔/秒） | 3 | 1 | 1 | 1 |
| sysMem%（内存阈值） | 70 | 50 | 30 | 20 |
| highWmark | 15 | 10 | 5 | 2 |
| lowWmark | 10 | 6 | 3 | 1 |
| T（冷热阈值） | 3 | 1 | 1 | 1 |
| maxThreads | 1 | 1 | 2 | 2 |
| swapFlag | no | no | no | no |

---

## 11. 清理与卸载

### 11.1 移除 etmem 标签（停止管理）

```bash
kubectl -n etmem-test label pod memory-hog etmem.openeuler.io/enable-
```

**预期效果：** Operator 自动删除对应的 EtmemPolicy。

```bash
# 验证 Policy 已删除
kubectl get etmempolicy -A
```

### 11.2 删除测试工作负载

```bash
kubectl delete namespace etmem-test
```

### 11.3 卸载 etmem-operator

```bash
helm uninstall etmem-operator -n etmem-system
```

### 11.4 清理 CRDs（可选）

> ⚠️ Helm uninstall **不会**自动删除 CRDs。如需完全清理：

```bash
kubectl delete crd etmempolicies.etmem.openeuler.io
kubectl delete crd etmemnodestates.etmem.openeuler.io
```

### 11.5 清理命名空间

```bash
kubectl delete namespace etmem-system
```

### 11.6 清理镜像（可选）

```bash
sudo nerdctl --namespace k8s.io rmi etmem-operator:v0.2.0
sudo nerdctl --namespace k8s.io rmi etmem-agent:v0.2.0
```

---

## 12. 常见问题排查

### Q1: Pod 状态为 ImagePullBackOff

**原因：** K8s 节点找不到镜像。

**解决：**
```bash
# 确认镜像在节点上存在（注意 namespace 必须是 k8s.io）
sudo nerdctl --namespace k8s.io images | grep etmem

# 如果不存在，重新构建或导入
sudo nerdctl --namespace k8s.io build -t etmem-operator:v0.2.0 -f Dockerfile.operator .
```

### Q2: Operator 日志报错 "failed to list/watch"

**原因：** CRD 未正确安装或 RBAC 权限不足。

**解决：**
```bash
# 重新检查 CRD
kubectl get crd | grep etmem

# 检查 ServiceAccount
kubectl -n etmem-system get serviceaccount

# 检查 ClusterRole/ClusterRoleBinding
kubectl get clusterrole | grep etmem
kubectl get clusterrolebinding | grep etmem
```

### Q3: 添加 label 后没有自动创建 EtmemPolicy

**可能原因：**
1. Operator 未运行或异常
2. Label 拼写错误

**排查：**
```bash
# 确认 label 正确
kubectl -n etmem-test get pod memory-hog --show-labels

# 确认 Operator 日志
kubectl -n etmem-system logs deploy/etmem-operator --tail=30

# label 必须是 etmem.openeuler.io/enable=true（注意大小写）
```

### Q4: Agent 日志中无 "Matched pod" 信息

**可能原因：**
1. Agent 未运行在 Pod 所在节点
2. EtmemPolicy 未创建
3. Agent 的 reconcileInterval 较长，需等待

**排查：**
```bash
# 确认 Pod 所在节点
kubectl -n etmem-test get pod memory-hog -o wide

# 确认该节点上有 Agent Pod
kubectl -n etmem-system get pods -o wide | grep agent
```

### Q5: Agent 找不到容器 PID

**可能原因：** cgroup v2 环境，或 cgroup 路径不匹配。

**排查：**
```bash
# 在节点上手动检查 cgroup 路径
# 获取 Pod UID
kubectl -n etmem-test get pod memory-hog -o jsonpath='{.metadata.uid}'

# 在节点上查找对应 cgroup（需要 ssh 到节点）
find /sys/fs/cgroup/memory/kubepods -name "cgroup.procs" | head -20
```

**cgroup v1 PID 解析路径说明：**

Agent 通过以下路径解析容器 PID：
1. 从 Pod metadata 获取 UID
2. 根据 QoS 等级确定路径前缀：
   - Guaranteed: `/sys/fs/cgroup/memory/kubepods/<pod-uid>/`
   - Burstable: `/sys/fs/cgroup/memory/kubepods/burstable/<pod-uid>/`
   - BestEffort: `/sys/fs/cgroup/memory/kubepods/besteffort/<pod-uid>/`
3. 在路径下找到 `<container-id>/cgroup.procs` 文件
4. 从文件中读取 PID 列表

### Q6: etmemd 扫描成功但不 swap

**现象：** Agent 日志正常，VmSwap 始终为 0。

**这是我们在真实验证中发现的问题。** 原因可能是：
1. 系统内存使用率未达到 profile 阈值 — 尝试使用 `extreme` profile
2. swappiness 设置过低 — `sysctl vm.swappiness=60`
3. swap 设备未正确配置 — 检查 `swapon -s`
4. etmemd 版本与内核不匹配

### Q7: 多节点环境部署失败

**解决：**
1. 确保每个 Worker 节点都有镜像（参考 4.3 节）
2. 确保每个 Worker 节点都运行了 etmemd（数据面必需）
3. 确保每个 Worker 节点都加载了 etmem 内核模块（数据面必需）

### Q8: Helm 升级后配置不生效

```bash
# 使用 upgrade 而非 install
helm upgrade etmem-operator deploy/helm \
  --namespace etmem-system \
  --set agent.reconcileInterval=60s

# 确认 Pod 已重建
kubectl -n etmem-system get pods
```

---

## 附录：完整验证流程速查

```bash
# === 环境检查 ===
kubectl version --short
helm version --short
go version
stat -fc %T /sys/fs/cgroup/

# === 部署 ===
helm install etmem-operator deploy/helm -n etmem-system --create-namespace

# === 检查部署 ===
kubectl -n etmem-system get pods
kubectl get crd | grep etmem

# === 创建测试 Pod ===
kubectl create ns etmem-test
cat <<EOF | kubectl apply -f -
apiVersion: v1
kind: Pod
metadata:
  name: memory-hog
  namespace: etmem-test
spec:
  containers:
  - name: stress
    image: busybox
    command: ["sh", "-c", "while true; do sleep 3600; done"]
EOF

# === 启用 etmem ===
kubectl -n etmem-test label pod memory-hog etmem.openeuler.io/enable=true

# === 验证 ===
kubectl get etmempolicy -A
kubectl -n etmem-system logs ds/etmem-agent --tail=30

# === 清理 ===
kubectl delete ns etmem-test
helm uninstall etmem-operator -n etmem-system
```

---

> 📖 更多关于「为什么本地验证不能替代真实环境验证」的详细说明，请参阅 [真实目标环境验证说明](./真实目标环境验证说明.md)。
